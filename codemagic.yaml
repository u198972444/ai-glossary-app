workflows:
  ai-glossary-ios:
    name: AI Word Lock Screen iOS
    max_build_duration: 60
    instance_type: mac_mini_m2
    scripts:
      - name: Test build
        script: |
          echo "Build started"
          ls -la
          echo "Build completed"
    artifacts:
      - build/ios/ipa/*.ipa
